# Design System & UI Overhaul Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish a unified design system with shadcn/ui (teal/emerald theme, Inter font, Notion-style sidebar) and fully restyle the Lender portal as proof of concept.

**Architecture:** Initialize shadcn/ui on the existing Radix UI + Tailwind CSS 4 stack. Define theme via CSS custom properties in globals.css. Build shared layout components (sidebar, page header, metric card). Then restyle each Lender page by replacing inline Tailwind with shadcn components.

**Tech Stack:** Next.js 15, React 19, Tailwind CSS 4, shadcn/ui (new-york style), Radix UI, Lucide React, Recharts, Inter font

**Spec:** `docs/superpowers/specs/2026-04-13-design-system-ui-overhaul.md`

**Constraints (from spec §7.4 & §9):**
- **No functionality changes.** This is purely visual. All API calls, state management, and business logic stay untouched.
- **Replace inline classes with shadcn components** where a component exists. Keep inline Tailwind for layout (flex, grid, padding, margin).
- **Preserve all responsive breakpoints** and mobile/tablet/desktop behavior.
- **Out of scope:** Dark mode, page transition animations, backend changes, Vendor/Admin portal page content restyling (only their layouts/sidebars are updated).
- **Login page path:** The actual route is `frontend/src/app/(auth)/login/` (route group with parentheses).

---

## File Structure

### New Files
- `frontend/components.json` — shadcn/ui configuration (auto-generated by init)
- `frontend/src/components/ui/*.tsx` — shadcn component files (button, card, input, label, table, badge, dialog, tabs, select, dropdown-menu, separator, skeleton, tooltip, avatar, sheet)
- `frontend/src/components/status-badge.tsx` — semantic status badge wrapper
- `frontend/src/components/page-header.tsx` — reusable page header
- `frontend/src/components/app-sidebar.tsx` — shared sidebar component

### Modified Files
- `frontend/src/app/globals.css` — full rewrite with shadcn theme variables
- `frontend/src/app/layout.tsx` — Inter font setup
- `frontend/src/app/lender/layout.tsx` — new sidebar + header
- `frontend/src/app/vendor/layout.tsx` — new sidebar + header
- `frontend/src/app/admin/layout.tsx` — new sidebar + header
- `frontend/src/components/metric-card.tsx` — restyle with design tokens
- `frontend/src/components/notification-bell.tsx` — restyle with design tokens
- `frontend/src/components/tier-badge.tsx` — restyle with design tokens
- `frontend/src/components/rating-stars.tsx` — restyle with design tokens
- `frontend/src/components/download-button.tsx` — restyle with shadcn Button
- `frontend/src/app/(auth)/login/page.tsx` — restyle login
- `frontend/src/app/(auth)/login/_components/*.tsx` — restyle login sub-components
- `frontend/src/app/lender/dashboard/page.tsx` — restyle
- `frontend/src/app/lender/dashboard/_components/lender-stats.tsx` — restyle
- `frontend/src/app/lender/dashboard/_components/payables-section.tsx` — restyle
- `frontend/src/app/lender/dashboard/_components/recent-requests-table.tsx` — restyle
- `frontend/src/app/lender/requests/page.tsx` — restyle
- `frontend/src/app/lender/requests/_components/request-table.tsx` — restyle
- `frontend/src/app/lender/requests/new/page.tsx` — restyle
- `frontend/src/app/lender/requests/new/_components/property-form.tsx` — restyle
- `frontend/src/app/lender/requests/new/_components/report-config-form.tsx` — restyle
- `frontend/src/app/lender/requests/new/_components/price-confirmation.tsx` — restyle
- `frontend/src/app/lender/requests/[id]/page.tsx` — restyle
- `frontend/src/app/lender/marketplace/page.tsx` — restyle
- `frontend/src/app/lender/marketplace/_components/marketplace-filters.tsx` — restyle
- `frontend/src/app/lender/marketplace/_components/result-card.tsx` — restyle
- `frontend/src/app/lender/listings/page.tsx` — restyle
- `frontend/src/app/lender/listings/[id]/page.tsx` — restyle
- `frontend/src/app/lender/listings/purchases/page.tsx` — restyle
- `frontend/src/app/lender/settings/page.tsx` — restyle

---

## Task 1: Initialize shadcn/ui and Configure Theme

**Files:**
- Modify: `frontend/src/app/globals.css`
- Modify: `frontend/src/app/layout.tsx`
- Create: `frontend/components.json` (auto-generated)

- [ ] **Step 1: Install shadcn/ui CLI and initialize**

Run from the frontend directory:

```bash
cd /home/yogidigital/projects/propeval/frontend
npx shadcn@latest init
```

When prompted, select:
- Style: **New York**
- Base color: **Neutral**
- CSS variables: **Yes**
- Tailwind CSS v4: **Yes** (auto-detected)
- Components directory: `src/components/ui`

This creates `components.json` and updates `globals.css` with CSS variable boilerplate.

- [ ] **Step 2: Configure the teal/emerald theme in globals.css**

Replace the entire contents of `frontend/src/app/globals.css` with:

```css
@import "tailwindcss";
@import "tw-animate/css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar-background: var(--sidebar-background);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --font-sans: var(--font-inter);
}

:root {
  --background: oklch(0.985 0.002 247.858);
  --foreground: oklch(0.145 0.005 247.858);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0.005 247.858);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0.005 247.858);
  --primary: oklch(0.627 0.108 174.555);
  --primary-foreground: oklch(0.985 0.002 247.858);
  --secondary: oklch(0.953 0.028 173.684);
  --secondary-foreground: oklch(0.265 0.045 174.555);
  --muted: oklch(0.946 0.003 264.542);
  --muted-foreground: oklch(0.556 0.014 247.858);
  --accent: oklch(0.953 0.028 173.684);
  --accent-foreground: oklch(0.265 0.045 174.555);
  --destructive: oklch(0.577 0.245 27.325);
  --destructive-foreground: oklch(0.985 0.002 247.858);
  --border: oklch(0.922 0.004 264.542);
  --input: oklch(0.872 0.006 264.542);
  --ring: oklch(0.627 0.108 174.555);
  --chart-1: oklch(0.627 0.108 174.555);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --radius: 0.75rem;
  --sidebar-background: oklch(1 0 0);
  --sidebar-foreground: oklch(0.145 0.005 247.858);
  --sidebar-primary: oklch(0.627 0.108 174.555);
  --sidebar-primary-foreground: oklch(0.985 0.002 247.858);
  --sidebar-accent: oklch(0.953 0.028 173.684);
  --sidebar-accent-foreground: oklch(0.265 0.045 174.555);
  --sidebar-border: oklch(0.922 0.004 264.542);
  --sidebar-ring: oklch(0.627 0.108 174.555);
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

Note: The CSS above shows the target theme. When `shadcn init` runs, it generates a boilerplate globals.css. Replace the generated color values with the teal/emerald oklch values above (keeping the structural format shadcn creates). If shadcn generates hex/hsl values instead of oklch, convert the teal palette: primary → `hsl(173, 80%, 30%)` / secondary → `hsl(173, 80%, 95%)` / accent → `hsl(173, 58%, 39%)`.

- [ ] **Step 3: Set up Inter font in root layout**

Modify `frontend/src/app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "PropEval",
  description: "Property valuation and legal reports marketplace",
  manifest: "/manifest.json",
  themeColor: "#0D9488",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "PropEval",
  },
  icons: {
    apple: "/icons/icon-192.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>{children}</body>
    </html>
  );
}
```

- [ ] **Step 4: Verify the dev server starts without errors**

```bash
cd /home/yogidigital/projects/propeval/frontend
npm run dev
```

Open `http://localhost:3020` — verify the page loads. Font should now be Inter. Background should be warm gray.

- [ ] **Step 5: Commit**

```bash
git add frontend/components.json frontend/src/app/globals.css frontend/src/app/layout.tsx frontend/package.json frontend/package-lock.json
git commit -m "feat: initialize shadcn/ui with teal/emerald theme and Inter font"
```

---

## Task 2: Install Core shadcn/ui Components

**Files:**
- Create: `frontend/src/components/ui/button.tsx`
- Create: `frontend/src/components/ui/card.tsx`
- Create: `frontend/src/components/ui/input.tsx`
- Create: `frontend/src/components/ui/label.tsx`
- Create: `frontend/src/components/ui/table.tsx`
- Create: `frontend/src/components/ui/badge.tsx`
- Create: `frontend/src/components/ui/dialog.tsx`
- Create: `frontend/src/components/ui/tabs.tsx`
- Create: `frontend/src/components/ui/select.tsx`
- Create: `frontend/src/components/ui/dropdown-menu.tsx`
- Create: `frontend/src/components/ui/separator.tsx`
- Create: `frontend/src/components/ui/skeleton.tsx`
- Create: `frontend/src/components/ui/tooltip.tsx`
- Create: `frontend/src/components/ui/avatar.tsx`
- Create: `frontend/src/components/ui/sheet.tsx`
- Create: `frontend/src/components/ui/textarea.tsx`

- [ ] **Step 1: Install all required shadcn components**

```bash
cd /home/yogidigital/projects/propeval/frontend
npx shadcn@latest add button card input label table badge dialog tabs select dropdown-menu separator skeleton tooltip avatar sheet textarea
```

Accept all prompts. This creates files under `src/components/ui/`.

- [ ] **Step 2: Verify components were installed**

```bash
ls frontend/src/components/ui/
```

Expected: all 16 component files listed above.

- [ ] **Step 3: Verify dev server still works**

```bash
cd /home/yogidigital/projects/propeval/frontend
npm run dev
```

No errors expected — components are installed but not yet imported anywhere.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/ui/ frontend/package.json frontend/package-lock.json
git commit -m "feat: install 16 core shadcn/ui components"
```

---

## Task 3: Create Custom Shared Components

**Files:**
- Create: `frontend/src/components/status-badge.tsx`
- Create: `frontend/src/components/page-header.tsx`
- Create: `frontend/src/components/app-sidebar.tsx`
- Modify: `frontend/src/components/metric-card.tsx`

- [ ] **Step 1: Create StatusBadge component**

Create `frontend/src/components/status-badge.tsx`:

```tsx
import { Badge } from "@/components/ui/badge";
import { cn } from "@/lib/utils";

const statusStyles: Record<string, string> = {
  PENDING: "bg-amber-100 text-amber-800 border-amber-200",
  SENT: "bg-blue-100 text-blue-800 border-blue-200",
  BROADCAST: "bg-blue-100 text-blue-800 border-blue-200",
  ACCEPTED: "bg-teal-100 text-teal-800 border-teal-200",
  IN_PROGRESS: "bg-indigo-100 text-indigo-800 border-indigo-200",
  SUBMITTED: "bg-purple-100 text-purple-800 border-purple-200",
  COMPLETED: "bg-emerald-100 text-emerald-800 border-emerald-200",
  RECEIVED: "bg-emerald-100 text-emerald-800 border-emerald-200",
  PUBLISHED: "bg-emerald-100 text-emerald-800 border-emerald-200",
  REJECTED: "bg-red-100 text-red-800 border-red-200",
  CANCELLED: "bg-gray-100 text-gray-800 border-gray-200",
  EXPIRED: "bg-gray-100 text-gray-800 border-gray-200",
  REVISION: "bg-orange-100 text-orange-800 border-orange-200",
  GTR_REVIEW: "bg-violet-100 text-violet-800 border-violet-200",
  BILLED: "bg-blue-100 text-blue-800 border-blue-200",
  PAID: "bg-emerald-100 text-emerald-800 border-emerald-200",
  NEW: "bg-gray-100 text-gray-700 border-gray-200",
  VERIFIED: "bg-blue-100 text-blue-700 border-blue-200",
  TOP_VALUER: "bg-amber-100 text-amber-700 border-amber-200",
};

interface StatusBadgeProps {
  status: string;
  className?: string;
}

export function StatusBadge({ status, className }: StatusBadgeProps) {
  const style = statusStyles[status] || "bg-gray-100 text-gray-800 border-gray-200";
  const label = status.replace(/_/g, " ");

  return (
    <Badge
      variant="outline"
      className={cn("rounded-full px-2.5 py-0.5 text-xs font-medium border", style, className)}
    >
      {label}
    </Badge>
  );
}
```

- [ ] **Step 2: Create PageHeader component**

Create `frontend/src/components/page-header.tsx`:

```tsx
import { cn } from "@/lib/utils";

interface PageHeaderProps {
  title: string;
  description?: string;
  children?: React.ReactNode;
  className?: string;
}

export function PageHeader({ title, description, children, className }: PageHeaderProps) {
  return (
    <div className={cn("flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between mb-6", className)}>
      <div>
        <h1 className="text-2xl sm:text-3xl font-bold tracking-tight text-foreground">{title}</h1>
        {description && (
          <p className="text-sm text-muted-foreground mt-1">{description}</p>
        )}
      </div>
      {children && <div className="flex items-center gap-2 mt-3 sm:mt-0">{children}</div>}
    </div>
  );
}
```

- [ ] **Step 3: Create AppSidebar component**

Create `frontend/src/components/app-sidebar.tsx`:

```tsx
"use client";

import { usePathname } from "next/navigation";
import Link from "next/link";
import { cn } from "@/lib/utils";
import { type LucideIcon } from "lucide-react";
import { Sheet, SheetContent, SheetTrigger, SheetTitle } from "@/components/ui/sheet";
import { Button } from "@/components/ui/button";
import { Separator } from "@/components/ui/separator";
import { Menu } from "lucide-react";

export interface NavItem {
  label: string;
  href: string;
  icon: LucideIcon;
}

export interface NavGroup {
  title: string;
  items: NavItem[];
}

interface AppSidebarProps {
  portalName: string;
  navGroups: NavGroup[];
  onLogout: () => void;
  mobileOpen: boolean;
  onMobileOpenChange: (open: boolean) => void;
  headerRight?: React.ReactNode;
}

function SidebarContent({
  portalName,
  navGroups,
  onLogout,
  pathname,
  onNavigate,
}: {
  portalName: string;
  navGroups: NavGroup[];
  onLogout: () => void;
  pathname: string;
  onNavigate?: () => void;
}) {
  return (
    <div className="flex flex-col h-full">
      {/* Logo area */}
      <div className="px-5 py-6">
        <h1 className="text-xl font-bold tracking-tight text-foreground">PropEval</h1>
        <p className="text-xs text-muted-foreground mt-0.5">{portalName}</p>
      </div>

      <Separator />

      {/* Navigation */}
      <nav className="flex-1 px-3 py-4 space-y-6 overflow-y-auto">
        {navGroups.map((group) => (
          <div key={group.title}>
            <p className="px-3 mb-2 text-xs font-medium uppercase tracking-wider text-muted-foreground">
              {group.title}
            </p>
            <div className="space-y-1">
              {group.items.map((item) => {
                const isActive = pathname === item.href || pathname.startsWith(item.href + "/");
                return (
                  <Link
                    key={item.href}
                    href={item.href}
                    onClick={onNavigate}
                    className={cn(
                      "flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition-colors duration-150",
                      isActive
                        ? "bg-secondary text-primary font-medium"
                        : "text-muted-foreground hover:bg-muted hover:text-foreground"
                    )}
                  >
                    <item.icon className="h-5 w-5 shrink-0" />
                    {item.label}
                  </Link>
                );
              })}
            </div>
          </div>
        ))}
      </nav>

      <Separator />

      {/* Footer */}
      <div className="px-3 py-4">
        <button
          onClick={onLogout}
          className="flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-destructive hover:bg-destructive/10 transition-colors duration-150 w-full"
        >
          Logout
        </button>
      </div>
    </div>
  );
}

export function AppSidebar({
  portalName,
  navGroups,
  onLogout,
  mobileOpen,
  onMobileOpenChange,
  headerRight,
}: AppSidebarProps) {
  const pathname = usePathname();

  return (
    <>
      {/* Desktop sidebar */}
      <aside className="hidden lg:flex lg:w-[260px] lg:shrink-0 border-r border-border bg-sidebar-background flex-col">
        <SidebarContent
          portalName={portalName}
          navGroups={navGroups}
          onLogout={onLogout}
          pathname={pathname}
        />
      </aside>

      {/* Mobile sheet */}
      <Sheet open={mobileOpen} onOpenChange={onMobileOpenChange}>
        <SheetContent side="left" className="w-[260px] p-0">
          <SheetTitle className="sr-only">{portalName} Navigation</SheetTitle>
          <SidebarContent
            portalName={portalName}
            navGroups={navGroups}
            onLogout={onLogout}
            pathname={pathname}
            onNavigate={() => onMobileOpenChange(false)}
          />
        </SheetContent>
      </Sheet>

      {/* Mobile top bar */}
      <header className="lg:hidden flex items-center justify-between border-b border-border bg-card px-4 h-14">
        <div className="flex items-center gap-3">
          <Button
            variant="ghost"
            size="sm"
            onClick={() => onMobileOpenChange(true)}
            aria-label="Open menu"
          >
            <Menu className="h-5 w-5" />
          </Button>
          <span className="font-semibold text-foreground">{portalName}</span>
        </div>
        {headerRight && <div className="flex items-center gap-2">{headerRight}</div>}
      </header>

      {/* Desktop top bar */}
      <header className="hidden lg:flex items-center justify-end border-b border-border bg-card px-6 h-14">
        {headerRight}
      </header>
    </>
  );
}
```

- [ ] **Step 4: Restyle the MetricCard component**

Replace the entire contents of `frontend/src/components/metric-card.tsx` with:

```tsx
import { type LucideIcon } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { cn } from "@/lib/utils";

interface MetricCardProps {
  label: string;
  value: number | string;
  icon: LucideIcon;
  accentColor?: string;
  iconBgColor?: string;
}

const colorMap: Record<string, { border: string; iconBg: string; iconText: string }> = {
  teal: { border: "border-l-teal-500", iconBg: "bg-teal-50", iconText: "text-teal-600" },
  amber: { border: "border-l-amber-500", iconBg: "bg-amber-50", iconText: "text-amber-600" },
  emerald: { border: "border-l-emerald-500", iconBg: "bg-emerald-50", iconText: "text-emerald-600" },
  blue: { border: "border-l-blue-500", iconBg: "bg-blue-50", iconText: "text-blue-600" },
  purple: { border: "border-l-purple-500", iconBg: "bg-purple-50", iconText: "text-purple-600" },
  orange: { border: "border-l-orange-500", iconBg: "bg-orange-50", iconText: "text-orange-600" },
  red: { border: "border-l-red-500", iconBg: "bg-red-50", iconText: "text-red-600" },
};

export function MetricCard({
  label,
  value,
  icon: Icon,
  accentColor = "teal",
}: MetricCardProps) {
  const colors = colorMap[accentColor] || colorMap.teal;

  return (
    <Card className={cn("border-l-4 shadow-sm", colors.border)}>
      <CardContent className="flex items-center gap-4 p-5">
        <div className={cn("flex items-center justify-center h-10 w-10 rounded-lg", colors.iconBg)}>
          <Icon className={cn("h-5 w-5", colors.iconText)} />
        </div>
        <div>
          <p className="text-2xl font-bold text-foreground">{value}</p>
          <p className="text-sm text-muted-foreground">{label}</p>
        </div>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 5: Verify all components render**

```bash
cd /home/yogidigital/projects/propeval/frontend
npm run dev
```

No errors expected — components are created but not yet wired into pages.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/status-badge.tsx frontend/src/components/page-header.tsx frontend/src/components/app-sidebar.tsx frontend/src/components/metric-card.tsx
git commit -m "feat: add custom shared components (StatusBadge, PageHeader, AppSidebar, MetricCard)"
```

---

## Task 4: Restyle Portal Layouts (All Three)

**Files:**
- Modify: `frontend/src/app/lender/layout.tsx`
- Modify: `frontend/src/app/vendor/layout.tsx`
- Modify: `frontend/src/app/admin/layout.tsx`

- [ ] **Step 1: Restyle the Lender layout**

Replace the entire contents of `frontend/src/app/lender/layout.tsx` with:

```tsx
"use client";
import { useState } from "react";
import {
  LayoutDashboard,
  FileText,
  Search,
  ScrollText,
  ShoppingBag,
  Settings,
} from "lucide-react";
import { NotificationBell } from "@/components/notification-bell";
import { AppSidebar, type NavGroup } from "@/components/app-sidebar";
import { WebSocketProvider } from "@/contexts/websocket-provider";
import { useAuth } from "@/hooks/use-auth";

const lenderNav: NavGroup[] = [
  {
    title: "Main",
    items: [
      { label: "Dashboard", href: "/lender/dashboard", icon: LayoutDashboard },
      { label: "Requests", href: "/lender/requests", icon: FileText },
      { label: "Marketplace", href: "/lender/marketplace", icon: Search },
    ],
  },
  {
    title: "Reports",
    items: [
      { label: "Listings", href: "/lender/listings", icon: ScrollText },
      { label: "Purchased Reports", href: "/lender/listings/purchases", icon: ShoppingBag },
    ],
  },
  {
    title: "Account",
    items: [
      { label: "Settings", href: "/lender/settings", icon: Settings },
    ],
  },
];

export default function LenderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const { logout } = useAuth();

  return (
    <WebSocketProvider>
      <div className="flex min-h-screen bg-background">
        <AppSidebar
          portalName="Lender Portal"
          navGroups={lenderNav}
          onLogout={logout}
          mobileOpen={sidebarOpen}
          onMobileOpenChange={setSidebarOpen}
          headerRight={<NotificationBell />}
        />

        <div className="flex flex-col flex-1 min-w-0">
          <main className="flex-1 p-6">
            <div className="max-w-7xl mx-auto">{children}</div>
          </main>
        </div>
      </div>
    </WebSocketProvider>
  );
}
```

- [ ] **Step 2: Restyle the Vendor layout**

Read `frontend/src/app/vendor/layout.tsx` first, then replace with the same AppSidebar pattern. Use appropriate Lucide icons and nav groups for the Vendor portal:

```tsx
"use client";
import { useState } from "react";
import {
  LayoutDashboard,
  FileText,
  Upload,
  ScrollText,
  Map,
  DollarSign,
  User,
  Settings,
} from "lucide-react";
import { NotificationBell } from "@/components/notification-bell";
import { AppSidebar, type NavGroup } from "@/components/app-sidebar";
import { WebSocketProvider } from "@/contexts/websocket-provider";
import { useAuth } from "@/hooks/use-auth";

const vendorNav: NavGroup[] = [
  {
    title: "Main",
    items: [
      { label: "Dashboard", href: "/vendor/dashboard", icon: LayoutDashboard },
      { label: "Requests", href: "/vendor/requests", icon: FileText },
      { label: "Reports", href: "/vendor/reports", icon: Upload },
    ],
  },
  {
    title: "Marketplace",
    items: [
      { label: "Listings", href: "/vendor/listings", icon: ScrollText },
      { label: "Coverage Map", href: "/vendor/map", icon: Map },
      { label: "Pricing", href: "/vendor/pricing", icon: DollarSign },
    ],
  },
  {
    title: "Account",
    items: [
      { label: "Profile", href: "/vendor/profile", icon: User },
      { label: "Settings", href: "/vendor/settings", icon: Settings },
    ],
  },
];

export default function VendorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const { logout } = useAuth();

  return (
    <WebSocketProvider>
      <div className="flex min-h-screen bg-background">
        <AppSidebar
          portalName="Vendor Portal"
          navGroups={vendorNav}
          onLogout={logout}
          mobileOpen={sidebarOpen}
          onMobileOpenChange={setSidebarOpen}
          headerRight={<NotificationBell />}
        />

        <div className="flex flex-col flex-1 min-w-0">
          <main className="flex-1 p-6">
            <div className="max-w-7xl mx-auto">{children}</div>
          </main>
        </div>
      </div>
    </WebSocketProvider>
  );
}
```

- [ ] **Step 3: Restyle the Admin layout**

Read `frontend/src/app/admin/layout.tsx` first, then replace with the same AppSidebar pattern. Use appropriate Lucide icons and nav groups for the Admin portal:

```tsx
"use client";
import { useState } from "react";
import {
  LayoutDashboard,
  Users,
  Building2,
  Receipt,
  Tag,
  Layers,
  ShieldCheck,
  Settings,
  Activity,
} from "lucide-react";
import { NotificationBell } from "@/components/notification-bell";
import { AppSidebar, type NavGroup } from "@/components/app-sidebar";
import { WebSocketProvider } from "@/contexts/websocket-provider";
import { useAuth } from "@/hooks/use-auth";

const adminNav: NavGroup[] = [
  {
    title: "Overview",
    items: [
      { label: "Dashboard", href: "/admin/dashboard", icon: LayoutDashboard },
      { label: "Activity Log", href: "/admin/dashboard?tab=activity", icon: Activity },
    ],
  },
  {
    title: "Users",
    items: [
      { label: "Vendors", href: "/admin/accounts/vendors", icon: Users },
      { label: "Lenders", href: "/admin/accounts/lenders", icon: Building2 },
    ],
  },
  {
    title: "Finance",
    items: [
      { label: "Billing", href: "/admin/billing", icon: Receipt },
      { label: "Pricing Rules", href: "/admin/pricing", icon: Tag },
      { label: "Price Bands", href: "/admin/price-bands", icon: Layers },
    ],
  },
  {
    title: "Quality",
    items: [
      { label: "Reviews", href: "/admin/quality-reviews", icon: ShieldCheck },
    ],
  },
  {
    title: "System",
    items: [
      { label: "Settings", href: "/admin/settings", icon: Settings },
    ],
  },
];

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const { logout } = useAuth();

  return (
    <WebSocketProvider>
      <div className="flex min-h-screen bg-background">
        <AppSidebar
          portalName="Admin Portal"
          navGroups={adminNav}
          onLogout={logout}
          mobileOpen={sidebarOpen}
          onMobileOpenChange={setSidebarOpen}
          headerRight={<NotificationBell />}
        />

        <div className="flex flex-col flex-1 min-w-0">
          <main className="flex-1 p-6">
            <div className="max-w-7xl mx-auto">{children}</div>
          </main>
        </div>
      </div>
    </WebSocketProvider>
  );
}
```

- [ ] **Step 4: Fix AppSidebar header bar rendering**

The AppSidebar component renders the header bars, but they are siblings of the sidebar in the flex container. The layout wraps `<main>` in a `flex-col flex-1` div. The desktop header bar is rendered inside AppSidebar but needs to be inside the main content column. Refactor AppSidebar to separate the sidebar from the headers:

Update `frontend/src/components/app-sidebar.tsx` — split the return into two exports: `AppSidebar` (just the sidebar + mobile sheet) and `AppHeader` (just the header bars). Then update all three layouts to render them in the correct positions:

```tsx
// Add this new export to app-sidebar.tsx:

export function AppHeader({
  portalName,
  onMenuClick,
  children,
}: {
  portalName: string;
  onMenuClick: () => void;
  children?: React.ReactNode;
}) {
  return (
    <>
      {/* Mobile top bar */}
      <header className="lg:hidden flex items-center justify-between border-b border-border bg-card px-4 h-14">
        <div className="flex items-center gap-3">
          <Button
            variant="ghost"
            size="sm"
            onClick={onMenuClick}
            aria-label="Open menu"
          >
            <Menu className="h-5 w-5" />
          </Button>
          <span className="font-semibold text-foreground">{portalName}</span>
        </div>
        {children && <div className="flex items-center gap-2">{children}</div>}
      </header>

      {/* Desktop top bar */}
      <header className="hidden lg:flex items-center justify-end border-b border-border bg-card px-6 h-14">
        {children}
      </header>
    </>
  );
}
```

Remove the header rendering from the main `AppSidebar` component (remove the `headerRight` prop and both `<header>` elements). Remove the `mobileOpen`/`onMobileOpenChange` props from `AppSidebar` — instead, accept a `SheetTrigger` externally.

Then update each layout to:

```tsx
<div className="flex min-h-screen bg-background">
  <AppSidebar ... />

  <div className="flex flex-col flex-1 min-w-0">
    <AppHeader portalName="Lender Portal" onMenuClick={() => setSidebarOpen(true)}>
      <NotificationBell />
    </AppHeader>
    <main className="flex-1 p-6">
      <div className="max-w-7xl mx-auto">{children}</div>
    </main>
  </div>
</div>
```

- [ ] **Step 5: Verify all three portals visually**

```bash
cd /home/yogidigital/projects/propeval/frontend
npm run dev
```

Login as each user type and verify:
- Lender (`lender@abcl.com` / `lender123`): sidebar with icons, teal active state, proper grouping
- Vendor (`vendor@valuepro.com` / `vendor123`): sidebar with vendor-specific nav
- Admin (`admin@getitright.com` / `admin123`): sidebar with admin-specific nav
- Mobile: hamburger opens sheet drawer with same nav
- All pages still functional (no broken routes)

- [ ] **Step 6: Commit**

```bash
git add frontend/src/app/lender/layout.tsx frontend/src/app/vendor/layout.tsx frontend/src/app/admin/layout.tsx frontend/src/components/app-sidebar.tsx
git commit -m "feat: restyle all portal layouts with Notion-style sidebar"
```

---

## Task 5: Restyle Login Page

**Files:**
- Modify: `frontend/src/app/(auth)/login/page.tsx`
- Modify: login sub-components in `frontend/src/app/(auth)/login/_components/`

- [ ] **Step 1: Read all login page files**

Read these files to understand current structure:
- `frontend/src/app/(auth)/login/page.tsx`
- `frontend/src/app/(auth)/login/_components/login-form.tsx`
- `frontend/src/app/(auth)/login/_components/brand-panel.tsx`
- `frontend/src/app/(auth)/login/_components/portal-selector.tsx`

- [ ] **Step 2: Restyle the main login page**

Update `frontend/src/app/(auth)/login/page.tsx` to use the new design tokens:
- Replace `bg-blue-500` mobile logo bar with `bg-primary`
- Replace hardcoded text colors with `text-foreground`, `text-muted-foreground`
- Keep the two-column layout structure

- [ ] **Step 3: Restyle the BrandPanel**

Update `frontend/src/app/(auth)/login/_components/brand-panel.tsx`:
- Replace blue gradient background with teal gradient: `bg-gradient-to-br from-teal-600 to-emerald-700`
- Update text colors for contrast on teal background
- Ensure the brand panel uses design tokens where possible

- [ ] **Step 4: Restyle the LoginForm**

Update `frontend/src/app/(auth)/login/_components/login-form.tsx`:
- Replace `<input>` elements with shadcn `Input` component
- Replace `<label>` elements with shadcn `Label` component
- Replace the submit button with shadcn `Button` (default variant)
- Replace inline error styling with `text-destructive`
- Update focus states to use ring token

- [ ] **Step 5: Restyle the PortalSelector**

Update `frontend/src/app/(auth)/login/_components/portal-selector.tsx`:
- Replace hardcoded button styles with shadcn `Button` variants
- Active portal: `variant="default"`, inactive: `variant="outline"`

- [ ] **Step 6: Verify login flow**

Open `http://localhost:3020/login`. Verify:
- Brand panel has teal gradient
- Form inputs are styled with shadcn components
- Login works end-to-end (credentials still accepted)
- Mobile view: brand panel hidden, logo bar shows

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app/\(auth\)/
git commit -m "feat: restyle login page with teal theme and shadcn components"
```

---

## Task 6: Restyle Lender Dashboard

**Files:**
- Modify: `frontend/src/app/lender/dashboard/page.tsx`
- Modify: `frontend/src/app/lender/dashboard/_components/lender-stats.tsx`
- Modify: `frontend/src/app/lender/dashboard/_components/payables-section.tsx`
- Modify: `frontend/src/app/lender/dashboard/_components/recent-requests-table.tsx`

- [ ] **Step 1: Restyle the dashboard page**

Read and update `frontend/src/app/lender/dashboard/page.tsx`:
- Import `PageHeader` from `@/components/page-header`
- Import `Button` from `@/components/ui/button`
- Replace the heading + "Raise Request" button with:

```tsx
<PageHeader title="Dashboard" description="Overview of your valuation requests">
  <Button asChild>
    <a href="/lender/requests/new">
      <Plus className="h-4 w-4 mr-2" />
      Raise Request
    </a>
  </Button>
</PageHeader>
```

- Replace `bg-blue-600 text-white px-4 py-2 rounded-lg` button with shadcn Button

- [ ] **Step 2: Restyle LenderStats with new MetricCard**

Update `frontend/src/app/lender/dashboard/_components/lender-stats.tsx`:
- The MetricCard import stays the same, but update the color props to use the new `accentColor` string prop:

```tsx
<MetricCard label="Requests Raised" value={stats.requests_raised} icon={Send} accentColor="blue" />
<MetricCard label="Awaiting Reports" value={stats.awaiting_reports} icon={Clock} accentColor="amber" />
<MetricCard label="Reports Received" value={stats.reports_received} icon={FileText} accentColor="purple" />
<MetricCard label="Reports Accepted" value={stats.reports_accepted} icon={CheckCircle} accentColor="emerald" />
<MetricCard label="Listings Purchased" value={stats.listings_purchased} icon={ShoppingCart} accentColor="orange" />
```

- Update skeleton loading to use shadcn `Skeleton`:

```tsx
import { Skeleton } from "@/components/ui/skeleton";

// In the loading state:
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4">
  {Array.from({ length: 5 }).map((_, i) => (
    <Skeleton key={i} className="h-24 rounded-xl" />
  ))}
</div>
```

- [ ] **Step 3: Restyle PayablesSection**

Read and update `frontend/src/app/lender/dashboard/_components/payables-section.tsx`:
- Import `Card`, `CardHeader`, `CardTitle`, `CardContent` from `@/components/ui/card`
- Import `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` from `@/components/ui/table`
- Import `Skeleton` from `@/components/ui/skeleton`
- Replace `bg-white rounded-lg border p-6` containers with `<Card>` components
- Replace `bg-yellow-50 border border-yellow-200`, `bg-blue-50`, `bg-green-50` stat cards with MetricCard components using appropriate accent colors
- Replace `<table>` elements with shadcn Table components
- Replace loading `animate-pulse` divs with `<Skeleton>` components

- [ ] **Step 4: Restyle RecentRequestsTable**

Read and update `frontend/src/app/lender/dashboard/_components/recent-requests-table.tsx`:
- Import shadcn Card and Table components
- Import `StatusBadge` from `@/components/status-badge`
- Replace the inline status color mapping object with `<StatusBadge status={r.status} />`
- Replace `<table>` with shadcn Table components
- Replace `border rounded-lg p-4 hover:bg-gray-50` mobile cards with `<Card>` components with `hover:shadow-md transition-shadow`
- Replace `text-blue-600 hover:underline` links with `text-primary hover:underline`

- [ ] **Step 5: Verify dashboard**

Open `http://localhost:3020/lender/dashboard`. Verify:
- Metric cards have left accent strips and colored icon circles
- Tables use clean horizontal rules
- Payables section has card containers
- Skeletons show during loading
- Mobile view: cards stack properly, tables become card lists

- [ ] **Step 6: Commit**

```bash
git add frontend/src/app/lender/dashboard/
git commit -m "feat: restyle lender dashboard with shadcn cards, tables, and metric cards"
```

---

## Task 7: Restyle Lender Requests Pages

**Files:**
- Modify: `frontend/src/app/lender/requests/page.tsx`
- Modify: `frontend/src/app/lender/requests/_components/request-table.tsx`
- Modify: `frontend/src/app/lender/requests/new/page.tsx`
- Modify: `frontend/src/app/lender/requests/new/_components/property-form.tsx`
- Modify: `frontend/src/app/lender/requests/new/_components/report-config-form.tsx`
- Modify: `frontend/src/app/lender/requests/new/_components/price-confirmation.tsx`
- Modify: `frontend/src/app/lender/requests/[id]/page.tsx`

- [ ] **Step 1: Restyle the requests list page**

Read and update `frontend/src/app/lender/requests/page.tsx`:
- Import `PageHeader` from `@/components/page-header`
- Import `Button` from `@/components/ui/button`
- Import `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/tabs`
- Import `Skeleton` from `@/components/ui/skeleton`
- Replace the heading + button with `<PageHeader>` + `<Button>`
- Replace the tab buttons with shadcn `Tabs` component:

```tsx
<Tabs defaultValue="all" onValueChange={(v) => setActiveTab(v === "all" ? "" : v)}>
  <TabsList>
    <TabsTrigger value="all">All</TabsTrigger>
    <TabsTrigger value="PENDING">Pending</TabsTrigger>
    <TabsTrigger value="ACTIVE">Active</TabsTrigger>
    <TabsTrigger value="COMPLETED">Completed</TabsTrigger>
  </TabsList>
</Tabs>
```

- Replace `border-b-2 border-blue-600 text-blue-600` active tab with shadcn TabsTrigger styling

- [ ] **Step 2: Restyle the request table component**

Read and update `frontend/src/app/lender/requests/_components/request-table.tsx`:
- Import shadcn Table components
- Import `StatusBadge` from `@/components/status-badge`
- Import `Card`, `CardContent` from `@/components/ui/card`
- Remove the inline `statusColors` mapping object
- Replace `<span className="px-2 py-1 rounded-full text-xs font-medium ...">` with `<StatusBadge status={request.status} />`
- Replace `<table>` with shadcn `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`
- Replace mobile `border rounded-lg p-4 hover:bg-gray-50` cards with `<Card className="hover:shadow-md transition-shadow">`

- [ ] **Step 3: Restyle the new request page**

Read and update `frontend/src/app/lender/requests/new/page.tsx`:
- Import `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`
- Import `Button` from `@/components/ui/button`
- Import `PageHeader` from `@/components/page-header`
- Wrap the multi-step form in a `<Card className="max-w-2xl mx-auto">`
- Replace step indicator circles: active `bg-blue-600` → `bg-primary`, completed → `bg-primary`, pending → `bg-muted`
- Replace navigation buttons with shadcn `Button` variants

- [ ] **Step 4: Restyle form sub-components**

Read and update each form component:

**property-form.tsx:**
- Import `Input` from `@/components/ui/input`
- Import `Label` from `@/components/ui/label`
- Import `Textarea` from `@/components/ui/textarea`
- Replace `<input>` with `<Input>`, `<label>` with `<Label>`, `<textarea>` with `<Textarea>`
- Replace `<select>` with shadcn `Select` component

**report-config-form.tsx:**
- Same component replacements
- Replace radio button styling with card-style selectable options using `<Card className={cn("cursor-pointer border-2", selected ? "border-primary" : "border-border")}>`

**price-confirmation.tsx:**
- Import `Card` components
- Replace inline price display styling with `<Card>` + design tokens
- Replace confirm button with shadcn `Button`

- [ ] **Step 5: Restyle the request detail page**

Read and update `frontend/src/app/lender/requests/[id]/page.tsx`:
- Import `Card`, `Button`, `StatusBadge`, `Separator`, `PageHeader`
- Replace all inline status badge styling with `<StatusBadge>`
- Replace `bg-white rounded-lg border p-6` containers with `<Card>`
- Replace `bg-blue-600 text-white` buttons with shadcn `Button`
- Replace `border border-blue-300 text-blue-600 rounded` secondary buttons with `<Button variant="outline">`
- Replace `text-blue-600 hover:underline` links with `text-primary hover:underline`

- [ ] **Step 6: Verify all request pages**

Test in browser:
- `/lender/requests` — tabs work, table displays, status badges colored correctly
- `/lender/requests/new` — multi-step form navigates, inputs styled, submit works
- `/lender/requests/[id]` — detail page renders with cards, status badges, action buttons
- Mobile: tables become cards, forms stack properly

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app/lender/requests/
git commit -m "feat: restyle lender requests pages with shadcn components"
```

---

## Task 8: Restyle Lender Marketplace

**Files:**
- Modify: `frontend/src/app/lender/marketplace/page.tsx`
- Modify: `frontend/src/app/lender/marketplace/_components/marketplace-filters.tsx`
- Modify: `frontend/src/app/lender/marketplace/_components/result-card.tsx`

- [ ] **Step 1: Restyle the marketplace page**

Read and update `frontend/src/app/lender/marketplace/page.tsx`:
- Import `PageHeader`, `Button`, `Card`, `Skeleton`
- Replace page heading with `<PageHeader title="Marketplace" description="Browse reports and vendors">`
- Replace view toggle buttons: active `bg-blue-600 text-white` → shadcn `Button` variant="default", inactive → `Button` variant="outline"
- Replace loading `animate-pulse` divs with `<Skeleton>` components
- Replace pagination buttons with shadcn `Button` variant="outline" size="sm"

- [ ] **Step 2: Restyle marketplace filters**

Read and update `frontend/src/app/lender/marketplace/_components/marketplace-filters.tsx`:
- Import `Card`, `CardContent` from `@/components/ui/card`
- Import `Input` from `@/components/ui/input`
- Import `Label` from `@/components/ui/label`
- Import `Button` from `@/components/ui/button`
- Wrap filters in `<Card className="shadow-sm mb-6"><CardContent className="p-4">`
- Replace filter pill radio buttons with shadcn-styled toggles:
  - Active: `bg-primary text-primary-foreground`
  - Inactive: `bg-muted text-muted-foreground hover:bg-muted/80`
- Replace `<input>` elements with `<Input>`
- Replace `<select>` elements with appropriate select styling
- Replace Search button with `<Button>`
- Replace Clear button with `<Button variant="ghost">`

- [ ] **Step 3: Restyle result cards**

Read and update `frontend/src/app/lender/marketplace/_components/result-card.tsx`:
- Import `Card`, `CardContent` from `@/components/ui/card`
- Import `Badge` from `@/components/ui/badge`
- Replace `bg-white border rounded-lg p-4 hover:shadow-md` with `<Card className="hover:shadow-md transition-shadow cursor-pointer">`
- Replace `bg-blue-100 text-blue-700` property type tags with `<Badge variant="outline" className="...">`
- Add left accent strip: report cards `border-l-4 border-l-teal-500`, vendor cards `border-l-4 border-l-emerald-500`
- Replace hardcoded text colors with `text-foreground`, `text-muted-foreground`

- [ ] **Step 4: Verify marketplace**

Test in browser at `/lender/marketplace`:
- Filters in a card container with proper spacing
- Search/clear buttons styled
- Result cards have accent strips and proper elevation on hover
- Map markers still work
- View toggle (list/map/split) works
- Mobile: toggle between map and list

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/lender/marketplace/
git commit -m "feat: restyle lender marketplace with filter card and result cards"
```

---

## Task 9: Restyle Lender Listings Pages

**Files:**
- Modify: `frontend/src/app/lender/listings/page.tsx`
- Modify: `frontend/src/app/lender/listings/_components/listing-card.tsx`
- Modify: `frontend/src/app/lender/listings/[id]/page.tsx`
- Modify: `frontend/src/app/lender/listings/[id]/_components/report-preview-card.tsx`
- Modify: `frontend/src/app/lender/listings/[id]/_components/purchase-dialog.tsx`
- Modify: `frontend/src/app/lender/listings/[id]/_components/update-request-dialog.tsx`
- Modify: `frontend/src/app/lender/listings/[id]/_components/nearby-request-dialog.tsx`
- Modify: `frontend/src/app/lender/listings/purchases/page.tsx`

- [ ] **Step 1: Restyle listings browse page**

Read and update `frontend/src/app/lender/listings/page.tsx`:
- Import `PageHeader`, `Button`, `Card`, `Input`, `Skeleton`
- Replace page heading with `<PageHeader>`
- Replace filter inputs with `<Input>` components
- Replace view toggle buttons with shadcn `Button` variants
- Replace pagination buttons with shadcn `Button` variant="outline"

- [ ] **Step 2: Restyle listing card component**

Read and update `frontend/src/app/lender/listings/_components/listing-card.tsx`:
- Import `Card`, `CardContent`, `Badge`
- Replace inline card styling with `<Card className="hover:shadow-md transition-shadow">`
- Replace property type badges with `<Badge>`
- Update text colors to design tokens

- [ ] **Step 3: Restyle listing detail page**

Read and update `frontend/src/app/lender/listings/[id]/page.tsx`:
- Import `PageHeader`, `Card`, `CardHeader`, `CardTitle`, `CardContent`, `Button`, `StatusBadge`, `Separator`
- Replace `bg-white rounded-lg border p-6` containers with `<Card>`
- Replace inline buttons with shadcn `Button` variants
- Replace status badges with `<StatusBadge>`
- Replace `text-blue-600` links with `text-primary`
- Two-column layout: left card (report details) + right card (purchase/vendor info)

- [ ] **Step 4: Restyle listing detail sub-components**

Read and update each sub-component:

**report-preview-card.tsx:**
- Import `Card`, `Badge`, `StatusBadge`
- Replace inline card styling with shadcn Card
- Update badge/status styling

**purchase-dialog.tsx:**
- Import `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogFooter` from `@/components/ui/dialog`
- Import `Button` from `@/components/ui/button`
- Replace custom modal with shadcn Dialog
- Replace buttons with shadcn Button variants

**update-request-dialog.tsx:**
- Same Dialog pattern as purchase-dialog
- Replace form inputs with shadcn Input/Label/Textarea

**nearby-request-dialog.tsx:**
- Same Dialog pattern
- Replace form inputs with shadcn components

- [ ] **Step 5: Restyle purchased reports page**

Read and update `frontend/src/app/lender/listings/purchases/page.tsx`:
- Import `PageHeader`, `Card`, `CardContent`, Table components, `StatusBadge`, `Skeleton`
- Replace page heading with `<PageHeader title="Purchased Reports">`
- Replace `<table>` with shadcn Table components
- Replace mobile cards with `<Card>` components
- Replace status badges with `<StatusBadge>`
- Replace loading divs with `<Skeleton>` components

- [ ] **Step 6: Verify all listing pages**

Test in browser:
- `/lender/listings` — card grid, filters, map/list toggle
- `/lender/listings/[id]` — two-column layout, purchase dialog works, update/nearby dialogs work
- `/lender/listings/purchases` — table displays, download works
- Mobile: all pages responsive

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app/lender/listings/
git commit -m "feat: restyle lender listings and purchase pages with shadcn components"
```

---

## Task 10: Restyle Lender Settings

**Files:**
- Modify: `frontend/src/app/lender/settings/page.tsx`
- Modify: `frontend/src/app/lender/settings/_components/users-tab.tsx`
- Modify: `frontend/src/app/lender/settings/_components/template-builder.tsx`
- Modify: `frontend/src/app/lender/settings/_components/notification-prefs.tsx`
- Modify: `frontend/src/app/lender/settings/_components/lender-config-tab.tsx`

- [ ] **Step 1: Restyle the settings page with shadcn Tabs**

Read and update `frontend/src/app/lender/settings/page.tsx`:
- Import `PageHeader` from `@/components/page-header`
- Import `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` from `@/components/ui/tabs`
- Replace the heading with `<PageHeader title="Settings">`
- Replace the custom tab buttons with shadcn Tabs:

```tsx
<PageHeader title="Settings" />

<Tabs defaultValue="users">
  <TabsList>
    <TabsTrigger value="users">Users</TabsTrigger>
    <TabsTrigger value="template">Report Template</TabsTrigger>
    <TabsTrigger value="notifications">Notifications</TabsTrigger>
    <TabsTrigger value="config">Configuration</TabsTrigger>
  </TabsList>
  <TabsContent value="users"><UsersTab /></TabsContent>
  <TabsContent value="template"><TemplateBuilder /></TabsContent>
  <TabsContent value="notifications"><NotificationPrefs /></TabsContent>
  <TabsContent value="config"><LenderConfigTab /></TabsContent>
</Tabs>
```

- [ ] **Step 2: Restyle each settings tab component**

Read and update each tab:

**users-tab.tsx:**
- Import shadcn Table, Button, Card, Dialog, Input, Label
- Replace user list table with shadcn Table
- Replace "Invite User" button with shadcn Button
- Replace invite dialog with shadcn Dialog

**template-builder.tsx:**
- Import Card, Input, Label, Button, Separator
- Wrap template builder in Card
- Replace form inputs with shadcn Input/Label
- Keep drag-and-drop functionality, just restyle the items with Card components

**notification-prefs.tsx:**
- Import Card, CardContent, CardHeader, CardTitle
- Wrap preference groups in Card components
- Style toggle switches (keep existing pattern, update colors to design tokens)

**lender-config-tab.tsx:**
- Import Card, Input, Label, Button
- Wrap config form in Card
- Replace inputs with shadcn components

- [ ] **Step 3: Verify settings**

Test at `/lender/settings`:
- All tabs switch correctly
- Users tab: table displays, invite dialog works
- Template tab: builder works, drag-and-drop functional
- Notifications tab: toggles work
- Config tab: form submits

- [ ] **Step 4: Commit**

```bash
git add frontend/src/app/lender/settings/
git commit -m "feat: restyle lender settings with shadcn tabs and form components"
```

---

## Task 11: Restyle Shared Components

**Files:**
- Modify: `frontend/src/components/notification-bell.tsx`
- Modify: `frontend/src/components/tier-badge.tsx`
- Modify: `frontend/src/components/rating-stars.tsx`
- Modify: `frontend/src/components/download-button.tsx`
- Modify: `frontend/src/components/date-range-filter.tsx`

- [ ] **Step 1: Restyle NotificationBell**

Read and update `frontend/src/components/notification-bell.tsx`:
- Import `Button` from `@/components/ui/button`
- Import `Card` from `@/components/ui/card`
- Import `Separator` from `@/components/ui/separator`
- Replace the bell button with `<Button variant="ghost" size="sm">`
- Replace `bg-red-500 text-white rounded-full` unread badge — keep the red styling (it's semantic)
- Replace the dropdown container `bg-white rounded-lg shadow-lg border` with `<Card className="shadow-lg">`
- Replace `bg-blue-50` unread notification highlight with `bg-secondary`
- Replace `text-blue-600` "Mark all read" link with `text-primary`
- Keep the mobile full-screen overlay pattern but update colors

- [ ] **Step 2: Restyle TierBadge**

Read and update `frontend/src/components/tier-badge.tsx`:
- Import `Badge` from `@/components/ui/badge`
- Replace the custom `<span>` badge with shadcn `<Badge variant="outline">` and apply the tier-specific colors as className overrides
- Keep the existing tier config color mapping, just use it on the Badge component

- [ ] **Step 3: Restyle RatingStars**

Read and update `frontend/src/components/rating-stars.tsx`:
- Minimal changes — the star styling is already clean
- Replace `text-gray-300` empty star color with `text-muted-foreground/30`
- Keep `text-amber-400` filled color (semantic and correct)
- Keep the hover scale interaction

- [ ] **Step 4: Restyle DownloadButton**

Read and update `frontend/src/components/download-button.tsx`:
- Import `Button` from `@/components/ui/button`
- Import `DropdownMenu`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuTrigger` from `@/components/ui/dropdown-menu`
- Replace `bg-blue-600 text-white rounded-lg hover:bg-blue-700` with `<Button>`
- Replace the custom dropdown with shadcn DropdownMenu for the split button format selection

- [ ] **Step 5: Restyle DateRangeFilter**

Read and update `frontend/src/components/date-range-filter.tsx`:
- Import `Input` from `@/components/ui/input`
- Import `Label` from `@/components/ui/label`
- Replace `<input type="date">` with `<Input type="date">`
- Replace `<label>` with `<Label>`

- [ ] **Step 6: Verify shared components across portals**

Test:
- Notification bell dropdown on all portals
- Tier badges on marketplace/vendor pages
- Rating stars on vendor profile pages
- Download button on purchased reports page
- Date range filter on dashboard

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/notification-bell.tsx frontend/src/components/tier-badge.tsx frontend/src/components/rating-stars.tsx frontend/src/components/download-button.tsx frontend/src/components/date-range-filter.tsx
git commit -m "feat: restyle shared components with shadcn Button, Badge, DropdownMenu"
```

---

## Shared Patterns (apply throughout Tasks 6-11)

These patterns from the spec (§6) should be applied consistently whenever you encounter the relevant scenario in any page:

**Empty States:** When a page has no data (no requests, no listings), render:
```tsx
<div className="flex flex-col items-center justify-center py-12">
  <SomeLucideIcon className="h-12 w-12 text-muted-foreground mb-4" />
  <h3 className="text-lg font-semibold text-foreground">No items yet</h3>
  <p className="text-sm text-muted-foreground max-w-sm text-center mt-2">Description of what to do</p>
  <Button className="mt-4">CTA Button</Button>
</div>
```

**Loading States:** Replace all `animate-pulse` loading divs with shadcn `<Skeleton>` components matching the layout shape:
```tsx
<Skeleton className="h-24 rounded-xl" />  // for cards
<Skeleton className="h-8 w-full" />        // for table rows
```

**Toast Notifications:** Sonner is already installed. No changes needed — just ensure Sonner's position is bottom-right and any toast calls use consistent messaging.

**Transitions & Micro-interactions:** Apply to all interactive elements:
- Hover states: `transition-colors duration-150`
- Card hover: `hover:shadow-md transition-shadow duration-200`
- Button press: `active:scale-[0.98]` (handled by shadcn Button)
- No page transitions (keep navigation instant)

---

## Task 12: Final Visual QA and Polish

**Files:**
- Potentially any file from previous tasks (minor fixes)

- [ ] **Step 1: Full visual walkthrough — Lender portal**

Navigate through every Lender page in the browser and check:
- `/lender/dashboard` — metric cards, tables, chart
- `/lender/requests` — list, tabs, table/cards
- `/lender/requests/new` — form wizard, all steps
- `/lender/requests/[id]` — detail page, dialogs
- `/lender/marketplace` — filters, map, cards, split view
- `/lender/listings` — grid, map toggle, pagination
- `/lender/listings/[id]` — detail, purchase dialog
- `/lender/listings/purchases` — table, download
- `/lender/settings` — all tabs

For each page, check:
- Typography hierarchy (title > heading > body > small)
- Color consistency (teal primary, warm gray backgrounds, no stray blue-600)
- Card elevation (shadow-sm default, shadow-md on hover)
- Spacing consistency (p-5 cards, p-6 page, gap-4 grids)
- Status badges use StatusBadge component
- Buttons use shadcn Button variants
- No orphaned inline Tailwind blue-600 classes

- [ ] **Step 2: Mobile responsive check**

Open browser dev tools, test at 375px width (iPhone SE):
- Sidebar becomes hamburger menu (Sheet drawer)
- Tables transform to card lists
- Forms stack vertically
- Touch targets are 44px+ minimum
- No horizontal overflow

- [ ] **Step 3: Fix any remaining stray colors or inconsistencies**

Search the codebase for remaining hardcoded blue references in lender pages:

```bash
grep -r "bg-blue-600\|text-blue-600\|border-blue-600\|hover:bg-blue-700" frontend/src/app/lender/ frontend/src/components/
```

Replace any remaining instances with design tokens (`bg-primary`, `text-primary`, `border-primary`).

- [ ] **Step 4: Verify Vendor and Admin portals still work**

Login as vendor and admin — their pages should still be functional. Only the sidebar/header will look new; page content is unchanged (future phase).

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "fix: polish remaining color inconsistencies and visual QA fixes"
```

---

## Summary

| Task | Description | Files Changed | Est. Complexity |
|------|-------------|---------------|-----------------|
| 1 | Initialize shadcn/ui + theme + Inter font | 3 | Low |
| 2 | Install 16 shadcn components | 16 new | Low |
| 3 | Create shared components (StatusBadge, PageHeader, AppSidebar, MetricCard) | 4 | Medium |
| 4 | Restyle all 3 portal layouts | 4 | Medium |
| 5 | Restyle login page | 4 | Low |
| 6 | Restyle lender dashboard | 4 | Medium |
| 7 | Restyle lender requests (list + new + detail) | 7 | High |
| 8 | Restyle lender marketplace | 3 | Medium |
| 9 | Restyle lender listings (browse + detail + purchases) | 8 | High |
| 10 | Restyle lender settings | 5 | Medium |
| 11 | Restyle shared components | 5 | Medium |
| 12 | Final visual QA and polish | Variable | Low |
